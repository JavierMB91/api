openapi: 3.0.0
info:
  title: Documentación API del Proyecto
  description: Documentación completa de la API incluyendo autenticación, usuarios y productos.
  version: 1.0.0
  contact:
    email: admin@miempresa.com

servers:
  - url: http://localhost/api
    description: Servidor Local (XAMPP)

tags:
  - name: Auth
    description: Endpoints de autenticación y seguridad
  - name: Usuarios
    description: Gestión de usuarios del sistema
  - name: Productos
    description: Catálogo de productos
  - name: Pedidos
    description: Gestión de pedidos realizados

paths:
  # --- AUTH ---
  /auth/login:
    post:
      tags:
        - Auth
      summary: Iniciar sesión
      description: Envía credenciales para obtener un token de acceso.
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Login exitoso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
        '401':
          description: Credenciales inválidas

  # --- USUARIOS ---
  /usuarios:
    get:
      tags:
        - Usuarios
      summary: Obtener todos los usuarios
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Lista de usuarios recuperada
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Usuario'
    post:
      tags:
        - Usuarios
      summary: Crear un usuario
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UsuarioInput'
      responses:
        '201':
          description: Usuario creado exitosamente

  /usuarios/{id}:
    get:
      tags:
        - Usuarios
      summary: Obtener usuario por ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Detalle del usuario
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Usuario'
    put:
      tags:
        - Usuarios
      summary: Actualizar usuario
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UsuarioInput'
      responses:
        '200':
          description: Usuario actualizado
    delete:
      tags:
        - Usuarios
      summary: Eliminar usuario
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Usuario eliminado

  # --- PRODUCTOS (Ejemplo de otra entidad) ---
  /productos:
    get:
      tags:
        - Productos
      summary: Listar productos
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Producto'
    post:
      tags:
        - Productos
      summary: Crear producto
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProductoInput'
      responses:
        '201':
          description: Producto creado

  /productos/{id}:
    get:
      tags:
        - Productos
      summary: Obtener producto por ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Detalle del producto
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Producto'
    put:
      tags:
        - Productos
      summary: Actualizar producto
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProductoInput'
      responses:
        '200':
          description: Producto actualizado
    delete:
      tags:
        - Productos
      summary: Eliminar producto
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Producto eliminado

  # --- PEDIDOS ---
  /pedidos:
    get:
      tags:
        - Pedidos
      summary: Listar pedidos
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Lista de pedidos
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Pedido'
    post:
      tags:
        - Pedidos
      summary: Crear pedido
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PedidoInput'
      responses:
        '201':
          description: Pedido creado exitosamente

  /pedidos/{id}:
    get:
      tags:
        - Pedidos
      summary: Obtener pedido por ID
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Detalle del pedido
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Pedido'
    put:
      tags:
        - Pedidos
      summary: Actualizar pedido
      description: Actualiza el estado o los datos de un pedido existente.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PedidoInput'
      responses:
        '200':
          description: Pedido actualizado
    delete:
      tags:
        - Pedidos
      summary: Eliminar pedido
      description: Elimina un registro de pedido (usar con precaución).
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: Pedido eliminado

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    # Esquemas de Auth
    LoginRequest:
      type: object
      required:
        - email
        - password
      properties:
        email:
          type: string
          format: email
          example: admin@miempresa.com
        password:
          type: string
          format: password
          minLength: 6
          example: "123456"
    LoginResponse:
      type: object
      properties:
        token:
          type: string
          example: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
        expiresIn:
          type: integer
          example: 3600

    # Esquemas de Usuario
    Usuario:
      type: object
      properties:
        id:
          type: integer
          example: 10
        nombre:
          type: string
          example: "Ana García"
        email:
          type: string
          format: email
          example: "ana@ejemplo.com"
    UsuarioInput:
      type: object
      required:
        - nombre
        - email
      properties:
        nombre:
          type: string
          minLength: 2
          maxLength: 100
          pattern: '^[a-zA-ZáéíóúÁÉÍÓÚñÑ\s]+$'
          example: "Nuevo Usuario"
        email:
          type: string
          format: email
          maxLength: 255
          example: "nuevo@ejemplo.com"

    # Esquemas de Producto
    Producto:
      type: object
      properties:
        id:
          type: integer
          example: 101
        nombre:
          type: string
          example: "Laptop Gamer"
        precio:
          type: number
          format: float
          example: 1500.00
    ProductoInput:
      type: object
      required:
        - nombre
        - precio
      properties:
        nombre:
          type: string
          minLength: 3
          maxLength: 150
          pattern: '^[a-zA-Z0-9áéíóúÁÉÍÓÚñÑ\s\-\.]+$'
        precio:
          type: number
          minimum: 0.01

    # Esquemas de Pedido
    Pedido:
      type: object
      properties:
        id:
          type: integer
          example: 5001
        usuario_id:
          type: integer
          example: 10
        total:
          type: number
          format: float
          example: 250.50
        fecha:
          type: string
          format: date-time
          example: "2023-10-27T10:00:00Z"
    PedidoInput:
      type: object
      required:
        - usuario_id
        - total
      properties:
        usuario_id:
          type: integer
          minimum: 1
        total:
          type: number
          minimum: 0.01
        fecha:
          type: string
          format: date-time
